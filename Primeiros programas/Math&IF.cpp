#include <iostream>
#include <cmath>

int main() {

    using namespace std;

    double x = 3;
    double y = 4;
    double z;

    //MIN VALUE BETWEEN
    z = min(x,y);
    cout << z << endl;

    //MAX VALUE BETWEEN
    z = max(x,y);
    cout << z << endl;

    
    
    return 0;
}